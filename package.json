{
  "name": "tubitak-1831-proje-yazma",
  "version": "1.0.0",
  "private": true,
  "description": "TÜBİTAK 1831 Yeşil Dönüşüm proje başvurusu yazma web uygulaması (Node.js + React, Obsidian bilgi grafiği destekli)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "google:auth": "node server/scripts/google-auth.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
